#ifndef ENUMS_H
#define ENUMS_H

typedef enum systemOfMeasurement{
    metric, imperial
} systemOfMeasurement; 

typedef enum state {
    dis, en
} state;

typedef enum direction {
    left, right
} direction;

#endif